# «Честный Знак: Линия» — гейм-дизайн

Idle/clicker-игра про внедрение обязательной маркировки DataMatrix («Честный знак») на производственной линии.
Ниже — дизайн-решения, экономика, техника и роадмап. Реализовано всё, кроме явно помеченных пунктов «роадмап».

---

## 1. Главный крючок: дедлайн как кривая, а не рубильник

- Требование ЧЗ растёт **посекундно и по-штучно**: 0% → 100% к «Часу Ч», штраф «Проверка партии» при отставании доли маркировки;
- **Льготный период — 45 секунд**: без DM продаётся по полной цене, штрафы и брак за чтение кода не считаются (и рулон этикеток линию не стопорит). Только просрочка срока годности сразу в утиль;
- К «Часу Ч» внедрёнными считаются **принтер + камера + сервер**, иначе — «Бизнес закрыт»;
- **Банкротство**: баланс в минусе **10 секунд** (успел вернуться в плюс — отменяется);
- **Аудит** через 5 минут после внедрения: доля регистрации ≥ 90% и не минус → победа («Акт приёмки»), дальше — песочница.

Игрок экономически вынужден внедряться заранее, а не «в 9:59».

## 2. Фазы

- **Фаза I «Подготовка» (до Часа Ч):** закупка, рост, штрафы за маркировку.
- **Фаза II «Работа под ЧЗ»:** после внедрения — аудит. Победа → песочница (вторая линия, лазер, рекорды, бесконечность).

## 3. Кликер с первой секунды

- **Подача продукта:** клики по зоне «🏭 Производство» / кнопка «➕ Продукт»;
- **Руки заняты:** заказ кодов, ручная наклейка, докидывание брака на переклейку, ручная отправка партий — до полной автоматизации;
- Психология «Pay-to-Not-Suffer» в самой игре: вручную дёшево, но скучно; оборудование дорого, но авто.

## 4. Конвейер — отражение реального процесса ЧЗ

1. **Заказ кодов ЧЗ** — заказ партии кодов (200 шт., генерация ~15 с). «Автозаказ» (Цех) — порог = запас на **100 секунд работы линии** (1 шт/с → 100, 2 шт/с → 200).
2. **Печать этикеток** — типография (код прямо на продукт, ×3/код) или свой принтер (этикетки на рулон, база 3.6 шт/с; «Модельный ряд» до 20 ур., «Качество печати», «Ускоренная подача», «Автозамена»).
3. **Рулон и буфер** — рулон 90 / аппликатор 200→600, буфер печати виден отдельно; **рулон < 15 → после введения маркировки линия ждёт печати**.
4. **Наклейка** — человек (база 0.5 + 0.25 шт/с за уровень, до 100 ур.) и/или **принтер-аппликатор** (клеит сам со скоростью печати ×0.5); общая проклейка = сумма.
5. **Камера** — считывает **2 кода/с** (модельный ряд ReaderDM +20%/модель; «Промышленное освещение» +25% и без вспышки); ловит дубли/чужие/сроки; с ПО «Чёткость» — мыльный код в брак.
6. **Возвраты** — проскочивший мыльный DM «обнаруживается» покупателем: **возврат = штраф на полную цену** (шанс 50%).
7. **Отбраковка** — модуль на ленте: брак возвращается на переклейку (до 2 раз), иногда **пропускает** (−15% цены; снижается модельным рядом и «Двухступенчатой»).
8. **Склад** — вместимость (расширяется), рекламации (климат-контроль), наценка отгрузки («Отгрузка без потерь»).
9. **Контракты и партии** — заказчик появился → партия **под контракт**, собранная партия **автоматически закрывает контракт**; без контракта размер партии — автоматический, по производительности.
10. **Отчёты** — партиями (8 ед. или 12 с) через сервер ЧЗ; без сервера — вручную.

## 5. Продукты и скорость линий

| Продукт | Цена | Срок | Множитель линии |
|---|---|---|---|
| 🥫 Консервы | 14 ₽ | 36 мес | ×1.0 (эталон) |
| 🧴 Шампунь | 12 ₽ | 24 мес | ×1.36 |
| 🥛 Молоко | 9 ₽ | ~1 мес | ×2.42 |
| 🧃 Соки | 8 ₽ | 9 мес | ×3.06 |
| 🥤 Напитки | 6 ₽ | 6 мес | ×5.44 |

Скорость линии = **квадрат отношения цен** (в 2 раза дешевле — в 4 раза быстрее). Применяется к потоку, печати, наклейке и считыванию. Сортовые надбавки «Качества продукта» (10 ур.): премиум ×1.3 / люкс ×1.7 / элитный ×2.2.

## 6. Здоровье и ремонт

Износ модулей; **ремонт тем дороже, чем хуже состояние**; при полной поломке — «🆕 Купить новое». Сломанный модуль замедляет линию и работает вручную. Апгрейды: «Лазер» (без износа), «Автоочистка» (не ниже 30%), «Сервисная служба» (×0.5).

## 7. Режимы и рекорды

- 🟢 Лёгкий (700 ₽, 12 мин) / ⚪ Стандарт (400, 10) / 🔴 Хардкор (150, 8, штрафы ×1.5) / **∞ Бесконечный** (500, без дедлайна: требование растёт по времени, 10 минут до 100%);
- Рекорд: товар, произведено, промаркировано, дата, счёт = (промаркировано ×3 + произведено) × множитель сложности (×0.7 / ×1 / ×3 / ×1.5). Хранение: localStorage (file://) или node-сервер (`data/records.json`).

## 8. Экономика (актуальная, крутилки — `js/config.js`)

| Предмет | Цена | Эффект |
|---|---|---|
| Типография | 150 ₽ | код сразу на продукт, ×3/код |
| Свой принтер | 800 ₽ | этикетки (3.6/с), наклейка человеком |
| Камера | 800 ₽ | проверка 2 кода/с |
| Отбраковщик | 500 ₽ | авто-возврат брака |
| Сервер ОФД | 1500 ₽ | автоотправка отчётов |
| Вторая линия | 3000 ₽ | ×2 поток (видна: две дорожки) |
| Принтер-аппликатор | 2500 ₽ | клеит сам, рулон 200→600 |

Все улучшения продаются **в 3 раза дешевле** номинала. Прокачка — модальное окно по клику на шестерёнку 🔧.

## 9. DataMatrix — реальная генерация

Библиотека `datamatrix-svg` (MIT, `vendor/datamatrix.min.js`): настоящий ECC200. Хороший код — чёткий; брак печати — «мыльный» (размытый); после повторной переклейки — с царапинами. Офлайн-fallback — canvas-глиф.

## 10. Техника

- **Vanilla JS, без сборки**: `index.html` + `css/style.css` + `js/{config,state,records,engine,events,upgrades,ui,main}.js`, классические `<script>` (работает с `file://`);
- **Node-сервер** (`server.js`, без зависимостей): раздача статики, API рекордов (`/api/records`), автоподбор порта;
- Конвейер — DOM + `requestAnimationFrame`, боксы кликабельны; вся экономика — в `config.js`;
- Архитектура: станции конвейера — функции с зонами на ленте; апгрейды — данные в `UPGRADES`/`STAGES`; действие — через публичное API `Game.engine`;
- **Тесты**: `node tests/run.js` — 49 тестов в изолированном vm-контексте (заглушки DOM), сюиты: ядро, поток, контракты, настройки/баланс.

## 11. Почему будет весело

1. **Напряжение:** таймер, растущее требование, аудит, банкротство за 10 секунд минуса.
2. **Руки заняты:** заказ кодов, подача, наклейка, докид брака, отправки, ремонт.
3. **Визуальный фидбек:** статусы боксов, реальные DM, вспышки камеры, смаз/царапины дефектов, деньги и возвраты.
4. **Прокачка порождает проблемы:** этикетки → камера (шт/с) → партии → рулоны/буфер → вторая линия → склад → возвраты...
5. **Прогрессия после победы:** модельные ряды, лазер, склад, бесконечный режим, рекорды.

## 12. Роадмап

1. Плейтест баланса → тюнинг `config.js` (партия 10–15 минут).
2. Звук, туториал, тач-управление.
3. Престиж-режим «Год спустя» (смена продукта) и кросс-платформенные рекорды.
4. Бэкенд (Express + SQLite) для кросс-девайсных сохранений.